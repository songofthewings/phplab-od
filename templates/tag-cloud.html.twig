{% extends 'base.html.twig' %}

{% block main %}
    {% block cloud %}
        {% if(tag_cloud) %}
            {% for item in tag_cloud %}
                {{  item.name }}<br>
            {% endfor %}
        {% endif %}
    {% endblock %}
{% endblock %}
